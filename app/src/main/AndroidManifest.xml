<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.uff.caronet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.activities.TermsOfUseActivity"></activity>
        <activity android:name=".view.activities.RegLogActivity" />
        <activity android:name=".view.activities.NewRideActivity" />
        <activity android:name=".view.activities.RideDetail" />
        <activity android:name=".view.activities.MyRidesActivity" />
        <activity android:name=".view.activities.ProfileActivity" />
        <activity
            android:name=".view.activities.RidesActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.activities.ChatActivity" />
        <activity android:name=".view.activities.LoginActivity" />
        <activity android:name=".view.activities.RegisterActivity" />
        <activity android:name=".view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>